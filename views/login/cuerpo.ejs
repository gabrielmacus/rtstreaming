<style>
  body > section
  {
    display: flex;
    width: 100%;
    height: 100%;
    align-items: center;
  }
</style>
<script>

  $(document).on('submit', '#login-form',function (e) {

    e.preventDefault();
    $.ajax(

      {
        url:'/ingresar',
        method:'post',
        dataType:'json',
        data:$("#login-form").serialize(),
        success:function (e) {

          if(e.redirect)
          {
            location.href =e.redirect;
          }
          else
          {
            location.reload();
          }

        },
        error:error
      }
    )

  });



</script>

<%- partial(form) %>
