<div class="mobile-bar">
<span class="hamburger">
  <i class="material-icons">&#xE3C7;</i>
</span>
</div>
<nav>
<div class="wrapper">


  <a  title="<%= __("usuario.bienvenida",user) %>">
    <span class="text"><%= __("usuario.bienvenida",user) %></span>
  </a>

  <a class="<% if(typeof home !== 'undefined' && home=='active') {%> active <%} %>" href="/admin/" title="<%= __("navbar.irHome")%>">
    <span class="text"><%= __("navbar.irHome")%></span>
    <span class="line"></span>
  </a>


  <a class="<% if(typeof irTransmision !=='undefined' && irTransmision=='active') {%> active <%} %>" href="/" title="<%= __("navbar.irTransmision")%>">
    <span class="text"><%= __("navbar.irTransmision")%></span>
    <span class="line"></span>
  </a>


  <% /* Solo si tengo los permisos suficientes */%>
  <% if(user.level >= publicData.canReadUserLevel )
  {
  %>
  <a class="<% if(typeof gestionUsuarios  !== 'undefined' && gestionUsuarios=='active') {%> active <%} %>" href="/admin/usuarios" title="<%= __("navbar.gestionUsuarios")%>">
    <span class="text"><%= __("navbar.gestionUsuarios")%></span>
    <span class="line"></span>
  </a>
  <%
  }%>

  <a href="/salir" title="<%= __("navbar.cerrarSesion")%>">
    <span class="text"><%= __("navbar.salir")%></span>
    <span class="line"></span>
  </a>


</div>

</nav>
